# Conformance: modeled vs observed

*Phase 4.* Records every known divergence between the modeled timeline and a real `ld.so`,
with the glibc version and the reason. See `PROJECT_PLAN.md` §7.1.

Divergences are content, not embarrassment — a documented one is a fact about a loader, and
the UI links to this file from the conformance badge.
